import { Box, Typography } from "@mui/material";
import CompTable from "../../components/CompTable/CompTable";

export default function FightersPage() {
    return (
        <Box sx={{justifyContent:'left', m:5}}>    
            <CompTable />
        </Box>
    );
}